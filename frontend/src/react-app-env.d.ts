// Reference the types provided by react-scripts package,
// which is part of the default Create React App setup for TypeScript.
// This allows TypeScript to recognize custom global types provided by CRA,
// such as environment variables and other CRA-specific configurations.

/// <reference types="react-scripts" />
